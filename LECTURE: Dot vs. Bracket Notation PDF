
/**LECTURE: Dot vs. Bracket Notation
1. Using the object from the previous assignment, log a string like this to the
console: 'Finland has 6 million finnish-speaking people, 3 neighbouring countries
and a capital called Helsinki.'
2. Increase the country's population by two million using dot notation, and then
decrease it by two million using brackets notation. */

const myCountry = {
  county: `Croatia`,
  capital: `Zagreb`,
  language: `Croatian`,
  population: 4,
  neighbours: [`Monte negro`, `Serbia`, `BiH`, `Hungary`, `Slovenia`, `Italy`],
};

//1

console.log(
  `${myCountry.county} has ${myCountry.population} milion people and ${myCountry.neighbours.length} neighbouring countries and a capital called ${myCountry.capital}`
);

//2
myCountry.population += 2;
console.log(myCountry.population);

myCountry["population"] -= 2;
console.log(myCountry.population);
